{
  "name": "clean-tweetx",
  "version": "1.0.1",
  "private": true,
  "packageManager": "bun@1.2.22",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "bun run build && bun test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
